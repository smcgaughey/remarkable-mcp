{
  "name": "remarkable-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Write-only MCP server exposing reMarkable cloud upload to Claude.ai. Runs on Cloudflare Workers.",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.7.2",
    "wrangler": "^4.0.0"
  }
}
